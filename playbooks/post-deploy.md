# Post Deploy

Confirm served SHA/assets and API/worker release markers. Verify migrations, health, rollback target and a browser journey. Record what is proven and what still needs real user/device proof.

Use the relevant skills; follow the target repository’s local instructions when they are more specific.
