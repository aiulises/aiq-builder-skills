# Bug Fix

Reproduce the symptom with exact inputs. Trace upstream source and contract. Fix root cause in smallest scope. Add regression coverage for the failure, run relevant checks, and verify the user path.

Use the relevant skills; follow the target repository’s local instructions when they are more specific.
